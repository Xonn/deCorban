{% extends 'base.html.twig' %}

{% set title = 'Réinitialisation du mot de passe' %}

{% block body %}
<div class="wrapper">
    <div class="container inner">
        <div class="row">
            <div class="security-form col-lg-10 offset-lg-1 col-xl-8 offset-xl-2">
                <h2 class="section-title section-title-upper large section-title-underline text-center">Réinitialisation
                    du mot de passe</h2>
                <div class="space50"></div>
                <p>Un email vous a été envoyé, il contient un lien vous permettant de réinitialiser votre mot de passe.
                    Ce lien expire dans {{ tokenLifetime|date('g') }} heure(s).</p>
                <p>Si vous n'avez pas reçu l'email, verifiez vos spam ou <a
                        href="{{ path('app_forgot_password_request') }}">réessayez</a>.</p>
            </div>
        </div>
    </div>
</div>
{% endblock %}
{% if reply %}
<ul class="children">
{% endif %}
    <li>
        <div class="message">
            <div class="user icon icon-round">
                <img alt="" src="{{ vich_uploader_asset(user, 'imageFile') }}">
                <span class="badge bg-role">Corbies</span>
            </div>
            <div class="message-inner">
            <div class="info">
                <h6><a href="#">{{ user.username }}</a></h6>
                <div class="meta"> <span class="date">{{ comment.createdat|localizeddate('medium', 'none') }}</span>
                    {% if not reply %}
                        <span class="reply"><a href="#">Répondre</a></span> 
                    {% endif %}
                </div>
            </div>
                {{ comment.message }}
            </div>
        </div>
    </li>
{% if reply %}
</ul>
{% endif %}
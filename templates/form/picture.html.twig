{% use '@!EasyAdmin/form/bootstrap_4.html.twig' %}

{% block picture_widget -%}
    <{{ element|default('div') }} class="cuseeetom-file">
        <input id="galery_pictureFiles" name="galery_pictureFiles[]" type="file" multiple>
    </{{ element|default('div') }}>
{% endblock %}